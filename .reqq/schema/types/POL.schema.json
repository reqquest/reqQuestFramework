{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "if": { "properties": { "type": { "const": "POL" } }, "required": ["type"] },
  "then": {
    "required": ["category"],
    "properties": {
      "category": { "type": "string", "minLength": 2 }
    }
  }
}
