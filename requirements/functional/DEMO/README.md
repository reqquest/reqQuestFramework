# DEMO — example category

`DEMO` is a placeholder demonstrating the `functional/<CATEGORY>/` convention described in [`../../../docs/conventions.md`](../../../docs/conventions.md), section 2. In a real project, replace `DEMO` with your own functional categories (e.g. `AUTH`, `BOOKING`, `NOTIFY`) and remove this directory.
