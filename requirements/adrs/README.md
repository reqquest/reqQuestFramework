# ADR — Architecture Decision Records

Numbering convention adopted as the default in this starter kit: `ADR-<CATEGORY>-NNN` (see [`../../docs/conventions.md`](../../docs/conventions.md), section 2). Alternative allowed by the methodology: globally sequential `ADR-NNNN` without a category segment — the choice depends on whether the repository has enough decisions across different areas for segmentation to make sense.
